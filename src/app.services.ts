export const generateGoal = (req, res) => {
    // Business logic to generate goal coordinates
  };
  
  export const checkGoal = (req, res) => {
    // Business logic to check if goal is reached
  };
  